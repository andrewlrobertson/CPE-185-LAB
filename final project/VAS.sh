#!/bin/bash

#Kaicha's command line argument
sleep 10 #change number of second for delay or remove line if no delay is needed

tesseract math.jpg output
sleep 40

#Ehsan's command line argument
sleep 10 #change number of second for delay or remove line if no delay is needed

#Abdullah's command line argument
sleep 10 #change number of second for delay or remove line if no delay is needed

#With this script, as long as all of our programs and libraries are installed we should
#only have to launch this file for everything to run in order. Will likely edit this
#to have fully qualified file paths.